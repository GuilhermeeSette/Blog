<!DOCTYPE html>
<html>
head
  title Rails Treinamento
  = csrf_meta_tags
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
  = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
body
  .row
    .navbar.navbar-default.navbar-fixed-top.header
      .container-fluid
        .navbar-header
          a.navbar-brand
            = link_to 'Codditus', home_index_path,  class: "brand d-inline-block align-center"
        -if session[:user_id].nil?
          a
            = link_to fa_icon('user-plus', text: 'Sign up'), new_user_path,  class: "btn btn-success btn-lg text-uppercase nav-link pull-right btn-up"
          a
            = link_to fa_icon('sign-in', text: 'Sign in'), login_url,  class: " btn btn-primary btn-lg text-uppercase nav-link pull-right btn-in"
        -else
          a
          = link_to fa_icon('sign-out', text: 'Sign out'), sessions_destroy_path,  class: "btn btn-danger btn-lg text-uppercase nav-link pull-right btn-out"
  = yield
